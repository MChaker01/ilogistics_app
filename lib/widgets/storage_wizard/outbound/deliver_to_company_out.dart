import 'package:flutter/material.dart';

class DeliverToCompanyOut extends StatelessWidget {
  const DeliverToCompanyOut({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Outbound - Deliver to Company'),
    );
  }
}