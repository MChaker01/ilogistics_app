import 'package:flutter/material.dart';

class DnMaterialsOut extends StatelessWidget {
  const DnMaterialsOut({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Outbound - DN Materials'),
    );
  }
}