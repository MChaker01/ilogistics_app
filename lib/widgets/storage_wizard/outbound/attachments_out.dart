import 'package:flutter/material.dart';

class AttachmentsOut extends StatelessWidget {
  const AttachmentsOut({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Outbound - Attachments'),
    );
  }
}