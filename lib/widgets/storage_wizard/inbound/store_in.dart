import 'package:flutter/material.dart';

class StoreIn extends StatelessWidget {
  const StoreIn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Inbound - Store'),
    );
  }
}