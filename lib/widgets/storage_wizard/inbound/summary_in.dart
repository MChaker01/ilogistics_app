import 'package:flutter/material.dart';

class SummaryIn extends StatelessWidget {
  const SummaryIn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Inbound - Summary'),
    );
  }
}