import 'package:flutter/material.dart';

class AttachmentsIn extends StatelessWidget {
  const AttachmentsIn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Inbound - Attachments'),
    );
  }
}