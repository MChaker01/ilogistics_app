import 'package:flutter/material.dart';

class HardwareStatusIn extends StatelessWidget {
  const HardwareStatusIn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Inbound - Hardware Status'),
    );
  }
}